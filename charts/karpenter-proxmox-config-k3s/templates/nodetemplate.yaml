{{- range $name, $template := .Values.nodeTemplate }}
{{- if $template.enabled }}
---
apiVersion: karpenter.proxmox.sinextra.dev/v1alpha1
kind: {{ $template.kind }}
metadata:
  name: {{ $name }}
  labels:
    {{- include "karpenter-proxmox-config-k3s.labels" $ | nindent 4 }}
spec:
  {{- $template.spec | toYaml | nindent 2 }}
{{- end }}
{{- end }}
